name: ENV Variables

on: push

env:
  WF_ENV: ${{ secrets.testy_secret }}

jobs:
  log-env: 
    runs-on: ubuntu-latest
    steps:
      - name: log env variables
        run: |
          Write-Host "WF_ENV: ${env:WF_ENV}"
          Write-Host "GITHUB_REF: ${env:GITHUB_REF}"
        shell: pwsh